.panel.panel-info
  - if commentable.comments && commentable.comments.any?
    .panel-heading
      ' Comments
    .panel-body
      = render commentable.comments
  .panel-footer

    = link_to 'comment', comments_new_path(commentable: commentable.class.to_s.underscore, commentable_id: commentable.id), remote: true
    div id = ("comment_form_#{commentable.id}")
    
var new_comment_form = "#new_comment_form_<%= @commentable.class_underscore%>_<%=@commentable.id %>"

$(new_comment_form).html('<%= j render partial: 'form', locals: {commentable: @commentable, comment: @comment} %>')
